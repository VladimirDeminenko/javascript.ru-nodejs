{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "compile": "concurrently \"tsc -w -p ./server\"",
    "start": "set NODE_PATH=.; && nodemon ./dist/server/app.js"
  }
}
